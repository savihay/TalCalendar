# TalCalendar

מערכת שעות שבועית לטל (כיתה ד׳). אפליקציית עמוד יחיד, עברית RTL, בלי build ובלי framework.
פורסמת ב-GitHub Pages: https://savihay.github.io/TalCalendar/

## קבצים

| קובץ | תפקיד |
|---|---|
| `index.html` | **כל האפליקציה** – נתונים, לוגיקה, CSS ושלוש התצוגות (טבלה/הדפסה/מובייל) |
| `build-api.js` | מייצר את `api/schedule.json` – מריץ את הסקריפט של `index.html` ב-DOM מזויף (בלי להכפיל לוגיקה) |
| `api/schedule.json` | API סטטי לסוכני AI. נבנה אוטומטית, **לא לערוך ביד** |
| `api/README.md` | תיעוד ה-API + `function_declaration` מוכן ל-Gemini Live |
| `.github/workflows/api.yml` | מריץ `build-api.js` בכל push שנוגע ב-`index.html` |
| `sw.js`, `manifest.webmanifest`, `icon-*.png` | PWA (network-first למסמך, כדי ששינויים יגיעו מיד) |

## כללי עבודה

1. **מקור אמת יחיד.** כל התצוגות נבנות מ-`DAYS`. אין לכתוב מערכת שעות ב-HTML סטטי.
2. **אחרי כל שינוי נתונים: `node build-api.js`** ולקמט את `api/schedule.json` יחד עם `index.html`.
   (אם אין node בסביבה – ה-Action יבנה אחרי ה-push; רק אל תשאיר קומיט עם JSON ישן וסותר.)
3. **לדחוף ישירות ל-main בלי לשאול.** הודעות קומיט בעברית, שורה ראשונה קצרה.
4. **מגע קל.** השינויים כאן תמיד קטנים (הזזת מפגש, תזכורת ליום, תאריך). עורכים, מסתכלים על ה-diff,
   מקמטים, דוחפים. בלי טסטים, בלי הרצות headless, בלי השוואות live מול local.
   בדיקה אמיתית שמורה לשינוי בלוגיקה עצמה (חישוב תאריכים, מנגנון ה-overrides, גלגול היום).
5. **תאריכים הם לוגיקה, לא טקסט.** `from`/`until` קובעים אם משהו מוצג – לא מציגים אותם למשתמש.
   היוצא מן הכלל היחיד: `startsOn`, שמוצג כ"מתחיל ב-8.10".

## מבנה הנתונים (`index.html`, בערך שורות 945–1060)

```js
DAYS[0..5]            // ראשון עד שישי. { name, short, tab, end, uniform, lessons[], activities[] }
activity              // { time, title, tableTitle?, sub?, type, legendLabel?, from?, until?, startsOn?,
                      //   end?, bring?, duringVacation? }
SATURDAY              // שבת – לא מוצגת באפליקציה, נכללת ב-API בלבד (אינדקס 6)
VACATIONS[]           // { name, icon, start, end, back?, noVacation?, note? }  – תאריך מלא YYYY-MM-DD
WEEK_OVERRIDES[]      // שינוי חד-פעמי לשבוע אחד
```

`from`/`until`/`startsOn` נכתבים כ-`"MM-DD"` ונפתרים לשנת הלימודים הנוכחית (`resolveDate`),
כדי שהקובץ ימשיך לעבוד בשנה הבאה. חודש ≥ 8 = שנת הפתיחה, אחרת השנה שאחריה.

### שינוי חד-פעמי לשבוע

`weekOf` = התאריך המלא של **יום ראשון** של אותו שבוע. `day` = 0 לראשון … 5 לשישי.

```js
{ weekOf: "2026-09-13",
  remove:  [{ day: 0, title: "מפגש עם עינת" }],          // title חייב להתאים בדיוק, כולל אימוג'י
  add:     [{ day: 3, time: "18:00", title: "מפגש עם עינת", type: "einat", sub: "השבוע בלבד" }],
  lessons: [{ day: 2, at: 0, title: "מתמטיקה" }],        // החלפת שיעור לפי אינדקס
  notes:   [{ day: 4, icon: "📐", text: "ספר גיאומטריה" }] }  // תזכורת ליום, אחת ליום
```

זו הדרך הנכונה לכל "השבוע ספציפית…" – **לא נוגעים ב-`DAYS`**. שינוי קבוע ("מעכשיו והלאה")
כן נכנס ל-`DAYS`; אם הוא מתחיל בתאריך עתידי, מוסיפים לו `from`.

### חוגים בחופשה

יום שנופל בתוך `start..end` של חופשה מוצג כיום חופש, ומוצגים בו רק חוגים עם
`duringVacation: true` (כדורסל, צופים, עינת). חוגים שעוצרים בחופשות – בלי הדגל.
שינוי חד-פעמי שנוסף ב-`add` מוצג תמיד, גם ביום חופש.

`end` (שעת סיום החוג) ו-`bring` (מה להביא) הם אופציונליים ומשמשים בעיקר את ה-API.

### חופשות

יום שנופל בתוך `start..end` של חופשה מוצג כיום חופש בכל התצוגות.
ההחלפה בין שבועות היא בשבת (`weekStartSunday`), ואחרי 19:00 מוצג היום הבא (`DAY_ENDS_AT_HOUR`).

### סוג פעילות (`type`)

כל `type` חייב זוג כללי CSS – `.compact-act.<type>` ו-`.mobile-act-card.<type>` – עם משתני צבע
משלו ב-`:root`. בלי זה החוג מוצג בלי צבע.

## מלכודות

- **`isVisible` נבדק מול היום המוצג**, לא מול היום האמיתי, אחרת דפדוף לשבוע הבא מציג אותו ריק.
- **הפס התחתון (`buildLegend`) נשען על `weekActs`**, כדי שישקף גם שינויים חד-פעמיים.
- **`fitPrintRows`** מחשב את גובה השורות כדי שההדפסה תיכנס לעמוד A4 אחד לרוחב. אם מוסיפים
  הרבה תוכן לשורה, כדאי לקצר `tableTitle` במקום להילחם בגובה.
- **`api/schedule.json` חייב לצאת זהה בכל בנייה** (טווח קבוע, בלי חותמת זמן), אחרת ה-Action
  דוחף קומיט משלו ומתנגש.
